# Sets

A set is a well defined and distinkt colection of elements.  
$ A = \{1,2,3\}$


## Powersets

A powerset of $A$ constains all possible subsets of $A$:  
$\mathcal{P} (A) := \{C \mid C$ is subset of $A\}$